import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    background: {
      paper: "#292929",
    },
    text: {
      mainText: "#ffffff",
    },
    item: {
      main: "#ffffff",
    },
  },
});

export default theme;